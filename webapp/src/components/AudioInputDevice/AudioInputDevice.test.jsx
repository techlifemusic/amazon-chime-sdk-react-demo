import React from 'react'
import { render } from '@testing-library/react'

import AudioInputDevice from 'components/AudioInputDevice'

// :: ---

it('renders without errors', () => {
  render(<AudioInputDevice />)
})
